version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frontend-pedidos360
    ports:
      - "4200:80" # Expone Nginx en el puerto 4200 para que coincida con tu config de Azure
    restart: always